(function($){$.fn.extend({eqHeight:function(options){var els=this;var options=$.extend({bindResizeEvent:true,heightAdjuster:function($self){return $self;}},options||{});if(!els.length)return;if(els.data('eqheight')){els=els.find(els.data('eqheight'));}
function resize(els){var rows={};els.each(function(){var $el=$(this);var rowIndicator=$el.offset().top;if(typeof rows[rowIndicator]==='undefined'){rows[rowIndicator]=[];}
rows[rowIndicator].push(options.heightAdjuster($el));});$.each(rows,function(rowIndicator,columns){$.each(columns,function(index,$column){$column.height('auto');});if(columns.length>1){var tallest=0;$.each(columns,function(index,$column){var height=$column.height();if(height>tallest){tallest=height;}});$.each(columns,function(index,$column){$column.height(tallest);});}});}
if(options.bindResizeEvent){$(window).resize(function(){resize(els);});}
resize(els);return els;}});$(function(){$('[data-eqheight]').eqHeight({});});}(jQuery));
